server {
    listen 80;
    server_name _;
    root /usr/share/nginx/html;
    index index.html;

    # SPA-Fallback: unbekannte Pfade an index.html weiterreichen (Client-Side-Routing)
    location / {
        try_files $uri $uri/ /index.html;
    }
}
